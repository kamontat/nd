{
  "name": "nd-config",
  "version": "1.5.0",
  "description": "configuration of nd command",
  "dependencies": {
    "nd-logger": "./modules/nd-logger"
  },
  "changelog": {
    "1.5.0": "change behavior of event in config; will notify everytime without filter",
    "1.4.2": "fix config error when start on new computer",
    "1.4.1": "add support multiple config in oneline",
    "1.4.0": "config can be restore to default and initial with user prompt",
    "1.3.0": "not only load config; but also save back to file",
    "1.2.0": "configuration support comment with # character",
    "1.1.0": "fix and improve workflow when config not found",
    "1.0.0": "first release new config modules"
  }
}