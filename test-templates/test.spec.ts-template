import * as chai from "chai";
import "mocha";
chai.should();

// const addContext = require("mochawesome/addContext");

const rootName = "";

describe(rootName, function() {
  describe("", function() {
    it("should ...", function() {
      // working on this :)
    });
  });
});
